# TGPS

## 1. 来源

| 项 | 内容 |
|---|---|
| 源文件 | `/home/tom/桌面/中国铁塔动环监控系统_统一互联B接口技术规范_分页排版版.docx` |
| 原章节 | 数据结构定义 |
| 原文位置 | 表8 数据结构定义，抽取稿 L887-L890 |

## 2. 结构用途

TGPS 是协议表8中定义的GPS数据结构。当前16个主命令样例未直接使用该结构，因此归入“协议已规定但当前未实现/未映射”清单。

## 3. 字段定义

| 字段 | 类型 | 必填 | 说明 | 所属命令 | SPEC 编号 | 备注 |
|---|---|---|---|---|---|---|
| FSUID | long | 待确认 | FSU ID | 未在主命令样例出现 | SPEC-2016-STRUCT-TGPS-001 | 表8定义为long，与FSUID_LEN字符串常量存在语义差异 |
| Lag | float | 待确认 | 经度 | 未在主命令样例出现 | SPEC-2016-STRUCT-TGPS-002 | 原文写Lag，疑似Lng/Longitude拼写问题，进入UNKNOWN |
| Lat | float | 待确认 | 纬度 | 未在主命令样例出现 | SPEC-2016-STRUCT-TGPS-003 |  |

## 3.1 SPEC 规则明细

### [SPEC-2016-STRUCT-TGPS-001] FSUID

规则类型：协议规定  
协议来源：`/home/tom/桌面/中国铁塔动环监控系统_统一互联B接口技术规范_分页排版版.docx`；表8 数据结构定义，抽取稿 L887-L890  
原文摘要：TGPS包含FSUID，类型long。  
规则内容：TGPS.FSUID为long类型FSU ID。  
适用范围：GPS扩展能力。  
影响模块：当前无直接业务映射。  
实现要求：未确认前不得映射到char[FSUID_LEN]字符串字段。  
测试要求：如后续实现GPS能力，需单独fixture。  
备注：当前未实现。

### [SPEC-2016-STRUCT-TGPS-002] Lag

规则类型：待确认  
协议来源：`/home/tom/桌面/中国铁塔动环监控系统_统一互联B接口技术规范_分页排版版.docx`；表8 数据结构定义，抽取稿 L887-L890  
原文摘要：TGPS包含Lag，类型float，说明为经度。  
规则内容：原文拼写为Lag；是否应为Lng/Longitude需确认。  
适用范围：GPS扩展能力。  
影响模块：当前无直接业务映射。  
实现要求：未确认前保留原文Lag，不擅自改名。  
测试要求：如后续实现，需覆盖Lag/Lat字段名。  
备注：进入UNKNOWN清单。

### [SPEC-2016-STRUCT-TGPS-003] Lat

规则类型：协议规定  
协议来源：`/home/tom/桌面/中国铁塔动环监控系统_统一互联B接口技术规范_分页排版版.docx`；表8 数据结构定义，抽取稿 L887-L890  
原文摘要：TGPS包含Lat，类型float，说明为纬度。  
规则内容：TGPS.Lat为float类型纬度。  
适用范围：GPS扩展能力。  
影响模块：当前无直接业务映射。  
实现要求：未确认前不得引入主链路。  
测试要求：如后续实现GPS能力，需单独fixture。  
备注：当前未实现。

## 4. XML 示例

```xml
<!-- 原文未提供TGPS XML样例 -->
<TGPS>
  <FSUID/>
  <Lag/>
  <Lat/>
</TGPS>
```

## 5. 平台映射

| 协议字段 | 平台字段 | 表 / DTO / Entity | 备注 |
|---|---|---|---|
| FSUID | gpsFsuId | 待设计 | 当前未实现 |
| Lag | longitude | 待设计 | 字段名需厂家/协议确认 |
| Lat | latitude | 待设计 | 当前未实现 |

## 6. 不明确事项

Lag字段拼写、FSUID类型long与其他FSU ID字符串字段差异需确认。

## 7. 测试要求

当前不建立业务测试；若后续实现GPS能力，必须先补XML样例和字段确认。
